apiVersion: v1
kind: Service
metadata:
  name: agilemeterfront
  labels:
    service: agilemeterfront
spec:
  ports:
  - name: "4200"
    protocol: "TCP"
    port: 4200
    targetPort: 4200
  selector:
    service: agilemeterfront
status:
  loadBalancer: {}
